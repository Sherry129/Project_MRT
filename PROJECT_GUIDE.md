# 台北捷運人流預測專案 — 入門指南

> **這份文件給誰看？** 完全沒接觸過資料科學的讀者。看完你會知道：這個專案在做什麼、為什麼這樣設計、每個數字怎麼來的、以及如何在自己的電腦上重現全部結果。 **最後更新**：2026-08-20（Stage 0–9 完成；第 5 節各階段新增「▍具體邏輯與步驟」詳解與初學者例子；最終作品集報告=`MRT作品集報告.docx`，剩 Portfolio 網頁由作者更新）

------------------------------------------------------------------------

## 1. 一分鐘看懂這個專案

**問題**：能不能提前一天，準確預測台北捷運 119 個車站「明天各有多少人進站」？

**做法**：拿 2023 年到 2026 年、共 3.4 億筆的進出站紀錄，用三個世代的預測方法各做一次——古典統計模型（SARIMA）、機器學習（隨機森林、梯度提升樹）、深度學習（LSTM）——然後公平比較誰更準。

**真正想回答的研究問題**：捷運站有不同「個性」（住宅區的站、上班族的站、觀光客的站），**不同個性的站，適合的預測工具一樣嗎？** 如果規律的住宅站用簡單便宜的統計模型就夠準，只有複雜的觀光站才需要昂貴的新方法，那捷運公司就不必對全部車站用同一套系統——這是比「模型排名」更有用的實務結論。

**目前進度**：資料工程、探索分析、特徵工程、統計基準線、機器學習、深度學習、可解釋性與事件分析（Stage 0–8）全部完成，最佳成績是 LSTM 的 WAPE **3.99%**；成果整合（Stage 9）已完成 GitHub 三件套、剩最終報告。

> **2026-08-06 全面重跑**：本指南已依重跑結果更新。三項主要變更：(1) 站點分群由 3 類修正為 4 類（原命名規則使兩個晚峰群撞名）；(2) 分時剖面排除 6 天「延長營運日」，修正一個會捏造偽群的聚合缺陷；(3) **SARIMAX 修正 Kalman filter 狀態初始化缺陷，WAPE 由 6.07% 降至 4.34%**——此修正推翻了原版多處結論，詳見「模型成績總表」下方的說明。

------------------------------------------------------------------------

## 2. 先備知識小字典

看報告前，先弄懂這幾個詞就夠了。

**時間序列（time series）**：按時間排列的數字，例如「動物園站每天的進站人數」。預測時間序列 = 根據過去的數字猜未來的數字。

**一步預測（one-step-ahead）**：預測「明天」時，可以使用今天（含）以前的所有真實資料。每天都重複這件事。這模擬捷運公司實際的使用情境：每天下班後，用當天為止的資料預測明天。

**訓練 / 驗證 / 測試（train / validation / test）**：把資料照時間切三段。模型只准「讀」訓練段學規律；驗證段用來挑設定（例如樹要長多深）；測試段是期末考，**只考一次**，考前絕不給模型看。本專案的切法固定為：

| 段         | 期間                              | 用途             |
|------------|-----------------------------------|------------------|
| train      | 2023-01-29 ～ 2024-12-31          | 學規律           |
| validation | 2025 全年                         | 挑超參數         |
| test       | 2026-01-01 ～ 2026-05-31（151天） | 期末考，只考一次 |

**WAPE（加權絕對百分比誤差）**：本專案的主要評分方式。算法是「所有預測誤差的絕對值加總 ÷ 所有實際人數加總」。WAPE 4% 的意思大約是「平均每 100 個進站的人，模型猜錯 4 個」。為什麼不用更常見的 MAPE？因為 MAPE 是逐日算百分比再平均，小站某天只有 500 人、猜錯 300 人就是 60% 誤差，會把整體分數炸掉；WAPE 用總量當分母，對大站小站都公平。

**基準線（baseline）**：故意先做幾個「笨方法」當標尺。例如「明天 = 今天」（naive）、「明天 = 上週同一天」（seasonal naive）。任何高級模型至少要贏過笨方法，否則就是白忙。

**資料洩漏（data leakage）**：模型偷看了「預測當下不可能知道的資訊」，導致成績虛高。這是初學者最常犯、也是口試最愛問的錯誤。本專案有三條防洩漏鐵律，見第 6 節。

**SARIMA / SARIMAX**：古典統計時間序列模型，擅長捕捉「固定週期」（例如每 7 天一循環的週節律）。X 代表額外解釋變數（例如「今天放不放假」）。

**梯度提升樹（XGBoost / LightGBM）**：機器學習模型，用成百上千棵決策樹接力修正彼此的錯誤，擅長從表格型特徵中挖出複雜的非線性規律，是資料競賽的常勝軍。

**LSTM**：一種深度學習的神經網絡，專門讀「序列」，會自己決定該記住或忘掉多久以前的資訊。

**Global model（全站共用模型）／逐站模型（per-station model）**：兩種完全不同的「模型數量」策略。逐站模型（本專案的 SARIMAX）是**119 個獨立模型**，每站一份專屬參數，只對自己那一站的歷史負責。Global model（本專案的 LightGBM、LSTM）是**1 個模型服務全部 119 站**，靠站型、路網位置（或 LSTM 的 station embedding）等特徵讓模型「知道自己在跟哪一種站說話」，但所有站共用同一套權重／同一套分裂規則。這個差別直接決定了誰在什麼情境贏：全站共用的參數是「對訓練集裡多數樣本最好」的一種妥協，遇到只出現幾天的稀有事件（颱風、春節）或極端規律的少數站型，這種妥協會被平均掉、稀釋掉；逐站模型沒有這種妥協壓力，能把全部參數用力貼合這一站、這一種效應。詳見第 5 節 Stage 8「結果二」的具體案例。

**SHAP**：一種「解釋模型」的方法，可以回答「模型做這個預測時，各個特徵分別貢獻了多少」。

------------------------------------------------------------------------

## 3. 資料是什麼？從哪來？

**主資料**：臺北市資料大平臺公開的「臺北捷運各站分時進出量統計 OD」。可以想像捷運公司每天記一本流水帳：「幾月幾號、幾點、有多少人從 A 站進站、最後從 B 站出站」。每月一個 CSV 檔，每筆記錄是「日期 × 小時 × 進站站 × 出站站 → 人次」。

**採用範圍**：2023 年 1 月～ 2026 年 5 月，共 41 個月、1,247 天、119 個車站、約 3.44 億筆原始紀錄。**為什麼從 2023 開始？** 更早的資料有 COVID 疫情造成的結構性斷裂（運量腰斬又恢復），混入會讓模型比較變成「誰比較會處理疫情」而非「誰比較會預測捷運」，敘事就髒了。

**輔助資料**：政府行政機關辦公日曆表（判斷放假、補班、春節）、臺北市天然災害停班停課紀錄（找出 6 個颱風停班日）。

**資料工程成果**：原始 CSV 共 12.4GB，全部下載、驗證後轉成壓縮的 parquet 格式，只剩 207MB，內容一筆不少。分析時用 DuckDB（可以直接對檔案下 SQL 查詢的工具）處理，不必把 3.4 億筆全部塞進記憶體。

### 三個必須知道的資料陷阱（血淚盤點成果）

**（1）環狀線移交造成的雙重斷點**。2023 年 6 月起，環狀線 14 站的營運移交新北捷運，帳本出現兩個變化。**表面的變化是出站紀錄消失**：從此「看得到有人從環狀線進站，看不到有人在環狀線出站」。**但更深一層、也更該注意的是進站量本身也掉了一階**——這件事在後來的複查中才用資料驗證出來（見下方「移交後帳本記什麼」），因為進站量正是本專案的預測目標。這不是資料壞掉，是換了記帳單位。**本專案仍把預測目標定為「進站量」**，理由與影響量化見後兩小節。

**（2）0403 花蓮地震**。2024/4/3 07:58 地震後環狀線全線停駛，當日 17:00 板橋—新北產業園區段 5 站即以單線雙向恢復營運（班距 16 分，其後長期減班）；中和—大坪林段 6 站於 4/7 11:00 恢復；受損的板新、中原、橋和三站停駛至 12/12 中午全線通車，形成長達 252 天（自 4/4 起算；媒體多以 4/3 起算計 253 天）的真實零值。特徵表已加 `service_suspended` 旗標標記（實際編碼為「4/4–4/6 全環狀線＋三站至 12/11」的近似：板橋以西 5 站於 4/4–4/6 被誤標為停駛，15 個站日、佔全表約 0.01%，全落在訓練期，test 期恆為 0，不影響任何結果）。

**（3）跨年通宵**。每年 12/31 通宵營運，12 月的資料檔日期會延伸到隔年 1/1，且跨年夜運量高達 295 萬人次（平日約 215 萬）。

### 站名清理的三條規則

專案的站名清理只有三條規則，每條的前提都是「先弄清楚怪名字背後是什麼」，不做批次自動修正：

**規則一（機械式）：轉檔時對所有站名 `TRIM` 去除前後空白。**「 動物園 」和「動物園」對人眼是同一站，對程式是兩個不同字串，不清會讓同一站的量被拆散。這條沒有風險，因為空白不可能承載真實資訊。

**規則二（合併變體）：2023-05 過渡月的三個前綴變體併回本名。**該月出站欄出現「G大坪林」「O景安」「O頭前庄」三個帶路線代碼的名稱，與無前綴版本並存（推測依出站閘門所屬路線區分記錄）。判定它們是「同一實體的變體」有三個依據：只出現在過渡月這一個月、時間點與環狀線營運移交的行政變化吻合、與無前綴版本的量互補而非重複。所以用寫死的對照表 `{"G大坪林"→"大坪林", "O景安"→"景安", "O頭前庄"→"頭前庄"}` 併回。若不併，該月會憑空多出三個「新站」，而真正的大坪林出站量被低估。

**規則三（反向）：不合併「BL板橋」與「Y板橋」。**它們名字像變體，實際是板南線與環狀線兩個實體不同的車站——不同月台、不同閘門、不同需求形態，在路網圖上是兩個節點，全期保持區分。

三條合起來的原則：**合併與否不看名字像不像，看背後是不是同一個實體。**這也是為什麼對照表是明文寫死的三個項目，而不是「自動去掉所有前綴」的通用規則——通用規則會把 BL板橋和 Y板橋錯誤地併成一站。

### 移交後帳本記什麼？——環狀線斷點的機制與大小

**保留規則：有經過北捷路網的旅次就留下。** 這份資料是「臺北捷運（北捷）」自己的 OD 帳本。環狀線移交新北捷運後，一趟旅程會不會被記進這本帳，取決於它有沒有踏進北捷路網：

-   完全待在環狀線／新北捷運網內的旅次（例如幸福→新北產業園區）→ 歸新北捷運記錄，**從此不在這本帳裡**。
-   從環狀站進站、但轉乘進北捷路網的旅次（例如**大坪林（環狀線）→板橋→頂埔（板南線）**）→ 因為後半段在北捷網路，**仍然被算入**大坪林的進站量。

**怎麼知道規則是這樣？** 關鍵證據是純環狀站的進站量掉幅**不是 100%**。如果規則是「從環狀線閘門進站就整筆歸新北捷運」，這些站在北捷帳本上應該歸零；但實測它們只掉四到六成、還留下三到五成——留下的那部分只可能是「轉乘進北捷」的乘客。這排除了「進站營運商擁有整筆紀錄」的另一種解釋。

**斷點有多大？**（比較移交前後相鄰月 2023-05 → 2023-06，並以非環狀對照站同期約 −3.4% 的變化當作季節性基準扣除）

| 群組 | 代表站 | 5月→6月進站量 | 扣季節後真實掉幅 |
|------------------|------------------|------------------|------------------|
| 對照（非環狀） | 中山 | 1,307,537 → 1,258,994 | −0.3%（純季節） |
| 對照（非環狀） | 台北車站 | 3,983,779 → 3,854,015 | +0.2%（純季節） |
| **雙線 3 站** | 景安（橘線＋環狀） | 689,375 → 587,186 | **−11.4%** |
| **雙線 3 站** | 大坪林（綠線＋環狀） | 647,088 → 525,032 | **−15.4%** |
| **雙線 3 站** | 頭前庄（橘線＋環狀） | 164,066 → 130,665 | **−16.9%** |
| **純環狀 11 站** | 幸福 | 98,868 → 53,904 | **−42.0%** |
| **純環狀 11 站** | 中和 | 122,538 → 61,024 | −46.8% |
| **純環狀 11 站** | 新北產業園區 | 126,544 → 48,070 | −58.6% |
| **純環狀 11 站** | 新埔民生 | 53,456 → 15,905 | −66.8% |
| **純環狀 11 站** | Y板橋 | 217,320 → 63,527 | −67.3% |
| 群組平均 | 雙線 3 站 | — | **−14.6%** |
| 群組平均 | 純環狀 11 站 | — | **−49.7%** |

**為什麼雙線站只掉 15%、純環狀站掉 50%？** 大坪林、景安、頭前庄同時是綠線或橘線的車站，這些「本來就全程在北捷」的乘客完全不受影響，被切掉的只有純環狀來回那一小塊；純環狀站沒有這個北捷基本盤，凡是目的地留在新北捷運網內的乘客全部消失，只剩轉乘北捷的那一半。所以**兩群的掉幅差距，本身就是「北捷路網在該站客流中佔多少比重」的度量**——新埔民生、Y板橋掉近七成，代表這兩站的乘客絕大多數在新北捷運網內活動，很少轉進北捷。這是斷點順帶提供的路網結構洞察。

**一個容易誤會的細節：進站側用的是「無前綴」站名。** 進站欄全期都是 119 個沒有路線代碼的站名——大坪林就是「大坪林」，不區分「大坪林」與綠線識別的「G大坪林」（景安、頭前庄同理沒有 O 前綴）。G／O 前綴變體只出現在**出站**欄。所以移交前，進站欄的「大坪林」＝綠線＋環狀線全部進站；移交後同一個欄位名稱不變，但內容變成「綠線＋轉乘北捷的環狀線旅次」。**欄位沒變、數字的組成變了**，這正是斷點藏得比較深的原因。

重現方式：`python scripts/stage2b_ring_handover_diagnostic.py`（輸出 `reports/stage2b_ring_handover.csv`）。

### 為什麼不直接把環狀線排除在分析之外？

既然環狀線資料有斷點，最省事的做法不是整批剔除 14 站嗎？不是，理由有四：

**第一（最核心）：斷點的位置與模型的使用時段錯開了。**進站量雖然在 2023-06 有水準位移，但斷點落在訓練窗最前端——訓練資料從 2023-01-29 開始，斷點後還有 31 個月（訓練＋驗證）都在同一個記帳制度下，而測試期（2026 年 1–5 月）完全落在斷點之後。也就是說，模型學到的和被考的都是「移交後」這個一致的世界，跨制度預測的問題並不存在。排除等於丟掉 14 站（約 12% 的樣本）在制度穩定期完全可用的目標資料，去遷就一段早已被後續資料淹沒的歷史。正確的處理是外科手術式的：問題在哪裡，就只在哪裡處理——OD 網絡特徵（PageRank、流量強度）需要用到出站結構，所以只在這一處改用 2023-06 之後的 108 站結構計算，其餘分析照常涵蓋 119 站。

**第二：排除會傷害研究問題本身。**研究主軸是「不同站型上模型的相對優勢」，環狀線 14 站多屬住宅型的近郊站，整批剔除會讓住宅型樣本失真，結論代表性下降。實務面也一樣：捷運公司需要的是全路網的預測，缺 14 站的系統外部效度打折。

**第三：留下環狀線反而換來分析素材。**0403 地震停駛（受損三站 252 天；全線停駛部分於當日至 4/7 分階段恢復）成為 `service_suspended` 旗標，後來在 Stage 8 成為「哪個實驗不必做」的判斷依據——旗標在事件前恆為 0，與颱風 zero-shot 情境同構，可據結構直接推論對照實驗必然無資訊量，故不執行。如果一開始就排除環狀線，這個方法論判斷的素材不會存在。

**第四：排除沒有被放棄，而是降級為穩健性檢查。**主分析包含 119 站，另外重算了一份「排除 14 站」的成績。

> **⚠️ 此項待重跑（2026-08-06）**：下表是 2026-07-07 版本的數字，當時的 SARIMAX 尚未修正 Kalman filter 缺陷、分群仍為 3 類。**主分析數字已全面更新，但這份穩健性檢查尚未以新版重跑**，故下表僅供參考其「差異量級」，不可引用其絕對值。
>
> | 模型     | 全 119 站 WAPE  | 排除 14 環狀站 | 差異    |
> |----------|-----------------|----------------|---------|
> | SARIMAX  | 6.07%（已失效） | 6.11%          | +0.04pp |
> | LightGBM | 4.62%           | 4.63%          | +0.00pp |
> | LSTM     | 4.03%           | 4.04%          | +0.01pp |
>
> 差異全部 ≤0.04pp，**結論「主結論不依賴這 14 站」的方向仍可成立**——因為 14 個環狀站雖佔預測點數的 11.8%，卻只佔測試期總運量的 3.1%（都是小站），而 WAPE 是以流量加權的指標，此結構性理由不受重跑影響。但正式引用前應以新版重跑一次。

這正是穩健性檢查該有的樣子：**排除是用來驗證主結論不依賴這 14 站的工具，不是主設計。**「遇到資料瑕疵就整批剔除」是最粗暴、通常也是資訊損失最大的處理方式；先分清楚瑕疵影響到什麼、不影響什麼、影響有多大，再決定處理的最小範圍，是這個專案想展示的資料工程判斷。

**附帶一提，這段複查本身也是個教訓。**最初的盤點把這件事記成「只有出站受影響，進站完整無缺」，直到後來重新追問「移交後帳本到底記什麼」，才用資料發現進站量也有斷點。發現後的處理不是掩蓋或推翻，而是量化它、驗證主結論是否動搖、把修正寫進文件。資料分析的可信度不來自「資料很乾淨」，而來自**知道自己的資料哪裡不乾淨，並且說得出影響有多大**。

------------------------------------------------------------------------

## 4. 九個階段地圖

| 階段 | 做什麼 | 狀態 | 主要產出 |
|------------------|------------------|------------------|------------------|
| 0 | 資料盤點：先搞清楚拿到什麼 | 完成 | `scripts/stage0_data_audit.py` |
| 1 | 資料工程：下載、清理、壓縮 | 完成 | `scripts/stage1b_download_od.py`、parquet×41 |
| 2 | 探索性分析（EDA）：認識資料 | 完成 | `reports/stage2_eda_report.md`、圖1\~5 |
| 3 | 收斂研究問題 | 完成 | 主軸=站型×模型世代 |
| 4 | 特徵工程：做出建模用的表 | 完成 | `data/features/feature_table.csv` |
| 5 | 基準線+統計模型（SARIMAX） | 完成 | `reports/stage5_baseline_report.md` |
| 6 | 機器學習（RF、LightGBM）+消融實驗 | 完成 | `reports/stage6_ml_report.md` |
| 7 | 深度學習（LSTM） | 完成 | `reports/stage7_dl_report.md` |
| 8 | SHAP可解釋性+事件分析 | 完成 | `reports/stage8_shap_events.md`、圖6\~10 |
| 9 | 整合成果、GitHub公開 | 完成 | `README.md`、`requirements.txt`、`.gitignore`、`MRT作品集報告.docx` |

------------------------------------------------------------------------

## 5. 各階段白話詳解與重現方法

> **重現前的環境準備**：Windows + Python 3.10 以上。各階段需要的套件在下方個別列出；所有指令都在專案根目錄（`F:\Project_MRT`）執行。 繪圖若中文變成方框，是字型問題：腳本已內建 `Microsoft JhengHei`（Windows）與 `Noto Sans CJK`（Linux）設定。

### Stage 0–1：資料盤點與資料工程

**做什麼**：從臺北市資料大平臺下載 41 個月的 CSV，驗證每個月的日期範圍、站點數、筆數合理，轉成 parquet 後刪除原始 CSV 省空間。

**為什麼重要**：3.4 億筆資料直接用 Excel 或 pandas 硬開會當機。這一步展現的是「大資料的工程處理能力」——用對工具（DuckDB + parquet），一台普通筆電就能處理。

**重現**：

```         
pip install duckdb pandas
python scripts/stage1b_download_od.py    # 下載+轉檔+驗證，可中斷續跑，約需數小時（視網速）
```

產出 `data/parquet/od/od_YYYYMM.parquet` ×41（共約 207MB）。

**▍具體邏輯與步驟**

**Stage 0 盤點（`stage0_data_audit.py`）先回答四個問題**：檔案有幾筆、哪些欄位、什麼型態？時間從哪到哪、日期有沒有斷？每個欄位有多少缺失值？類別欄（站名、時段）有哪些不重複的值？工具上有個示範點：大 CSV 不能用 pandas 硬開，腳本用 DuckDB 直接對檔案下 SQL（免載入記憶體），並備了 pandas 分塊讀取的備用模式。「時段有哪些值」這一問就是在這裡發現營運時段是 21 個（02–04 時沒有班次）；「站名有哪些值」則埋下了後來逐月比對、抓到環狀線斷點的伏筆。

**Stage 1b 下載與清理（`stage1b_download_od.py`）**——對 2023-01 到 2026-05 的每一個月，腳本做同一套五步驟：

1.  **先看做過沒**：該月的 parquet 已存在就跳過。所以中斷後重跑，只會補做沒完成的月份——這叫「可續跑」設計，下載 41 個月不怕斷網。
2.  **下載**：先寫入 `.tmp` 暫存檔，完整下載成功才改名為正式檔名；失敗自動重試 3 次，每次等更久。為什麼要 `.tmp`？如果下載到一半斷線，留下的是明顯的殘檔而不是「看起來完整其實缺尾巴」的假檔案。
3.  **清理＋轉檔（一句 SQL 完成）**：用 DuckDB 把 CSV 轉成 parquet，途中做三件清理——(a) `TRIM(站名)` 去掉前後空白（例如「 動物園 」→「動物園」，不清理的話會被當成兩個不同的站）；(b) 過濾掉「人次是空值或負數」的異常列；(c) 把欄位轉成正確型態（日期轉 DATE、時段轉小整數、人次轉整數），型態正確才省空間、才能做日期運算。
4.  **驗證**：轉好的 parquet 要通過兩關——列數 ≥ 100 萬（正常每月約 800 萬列，只有幾千列代表下載到殘檔）；日期範圍必須落在該月內。**唯一例外**：12 月檔允許日期到隔年 1/1，因為跨年夜通宵營運的班次記在隔天凌晨——連驗證規則的例外都對應一個真實世界的原因。
5.  **通過驗證才刪原始 CSV**：省下 5 倍空間，且因為第 4 步把關過，刪掉是安全的。

**小例子（站名清理為什麼重要）**：2023-05 過渡月的出站欄同時出現「大坪林」和「G大坪林」（帶路線代碼的變體）。若不合併，統計「大坪林的出站量」會少算一半。腳本用對照表 `{"G大坪林"→"大坪林", "O景安"→"景安", "O頭前庄"→"頭前庄"}` 把變體併回。反例是「BL板橋」與「Y板橋」——它們是板南線與環狀線兩個實體不同的車站，**不可以**合併。清理的原則：先弄懂每個「怪名字」背後是變體還是不同實體，再決定合併或保留，絕不批次亂改。

### Stage 2：探索性資料分析（EDA）

**做什麼**：畫圖、算統計量，回答「這份資料長什麼樣？有什麼規律？有什麼坑？」

**主要發現（看懂 `reports/stage2_eda_report.md` 的鑰匙）**：

1.  **週節律是最強規律**：平日均量 215.5 萬、週末 172.9 萬（約平日八成）。平日進站呈早 8 點、晚 6 點雙尖峰；週末是中午到傍晚的單一高原。
2.  **極端值都有事件解釋**：三大高峰=兩個跨年夜+新北耶誕城開幕日；三大低谷=康芮、凱米、山陀兒三個颱風停班日（運量只剩平日一成）。
3.  **車站有「個性」**：把每站的分時進站曲線形狀做分群（k-means, k=4），自動分出住宅型（早尖峰，46 站）、就業型（晚尖峰——上班族「下班進站」返家，28 站）、**就業極端型**（晚峰占平日 53%的單一尖峰，7 站）、觀光轉運型（週末比平日忙，38 站）。這就是研究問題的來源。
4.  **OD 網絡高度集中**：最大站對是西門→台北車站（累計 945 萬人次）；用網絡分析算 PageRank（衡量站在路網中的「樞紐程度」），前幾名是台北車站、西門、市政府。

**重現**：

```         
pip install duckdb pandas matplotlib scikit-learn
python scripts/stage2_eda.py
```

產出報告、圖 1\~5、以及 `data/agg/` 六張聚合表。

**▍具體邏輯與步驟**

1.  **先聚合、再分析**：3.4 億筆明細不能直接畫圖。腳本先用 DuckDB 的 GROUP BY 把明細壓成六張小表——站×日進站量（後續建模的原料）、站×時段平均曲線、OD 總矩陣、系統×小時流量、逐月站點覆蓋（就是抓到環狀線斷點的那張表）、站點分群結果。之後所有圖表都從小表出發，秒開。

2.  **站點分群的具體做法**：對每一站，算出「平日 24 小時的平均進站量」＋「週末 24 小時的平均進站量」共 48 個數字，再除以自身總量變成「形狀」（佔比）——這一步叫正規化，目的是讓大站小站可比：台北車站每天 30 萬人、小站 3 千人，但如果兩站都是「早上 8 點進站占全日 12%」，它們的「個性」就是同一種。然後用 k-means 把 119 個 48 維向量分成 4 群。

3.  **k-means 的白話原理**：隨機放 4 個「群中心」，每站加入離自己最近的中心，再把中心移到群內平均位置，重複到不再變動。腳本跑 8 個不同的隨機起點取最好的一次（k-means 對起點敏感，多跑幾次是標準防呆）。

4.  **群的命名不是人工看圖，是規則化的**：對每一群算平日曲線的「早尖峰量（7–9 時合計）」與「晚尖峰量（17–19 時合計）」——早尖峰 \> 晚尖峰×1.25 → 住宅型（早上出門通勤，從家附近的站進站）；晚尖峰 \> 早尖峰×1.25 → 就業型（下班才從公司附近的站進站返家），其中晚尖峰 \> 45% 者再細分為**就業極端型**（純就業腹地，幾乎沒有非通勤客流稀釋曲線）；兩者都不突出但週末占比 \> 40% → 觀光轉運型。命名規則寫死在腳本裡，可重現、可辯護，並有 `assert` 防止兩群撞名。

5.  **k=4 這個數字有經過驗證**：elbow / silhouette / gap 三個指標其實**沒有一個指向 4**（分別指向 3、2、以及「無明確分群數」）。gap 統計量單調上升，代表 119 站的曲線是一條**連續光譜**而非四團分開的雲——在這種資料上「真正的 k 是多少」本身就是錯誤的問題。保留 k=4 的理由改為：它是能分離出「極端單一晚峰」型態的最小 k，且 Stage 6b 的消融實驗證實 **k=3 與 k=4 對預測效能無顯著差異**（差 0.003–0.07pp，遠小於調參噪音 0.52pp）。分群在本專案的功能是**分層評估與解釋**，不是提升精度。

6.  **有 6 天資料被排除在分群之外**：凌晨 02–04 時有流量的日子（3 次跨年 + 3 次臺北白晝之夜）。原因是聚合公式的分母是「該時段有資料列的天數」，而 02 時只有這 6 天有紀錄，會讓象山站 02 時的「日均」從 22.7 人次膨脹到 6,739 人次（放大 297 倍），並憑空造出一個由信義線東段站點組成的**偽群**。這 6 天在日層級資料中完整保留，只是不參與「典型日常節奏」的定義。

**小例子（為什麼「晚尖峰=就業站」）**：內湖科技園區的港墘站，早上幾乎沒人「進站」（大家是出站去上班），下午 6 點卻有全日近 19% 的人擠著進站（下班回家）。進站曲線的形狀，記錄的是「誰住在這、誰在這上班」。

### Stage 4：特徵工程

**做什麼**：把原始資料變成一張「模型讀得懂的大表」：`data/features/feature_table.csv`，148,393 列（119 站 × 1,247 天）× 27 欄。每一列代表「某站某天」，欄位就是給模型的線索（特徵），欄位定義詳見 `data/features/feature_dictionary.md`。

**特徵分四類（白話版）**：

1.  **日曆線索**：今天星期幾？放假嗎？春節嗎？補班嗎？颱風停班嗎？跨年夜嗎？
2.  **動量線索**：這站昨天多少人？上週同一天多少人？最近 7 天、28 天平均多少人？（模型最依賴的線索——「最近的水準」）
3.  **車站個性**：這站屬於住宅/就業/觀光哪一群？它在路網中的 PageRank、進出流量強度、連接了幾個站？
4.  **特殊旗標**：是不是環狀線？是不是地震停駛期間？

**重現**：

```         
pip install duckdb pandas numpy
python scripts/stage4_features.py
```

**▍具體邏輯與步驟（照 `stage4_features.py` 的四個函式）**

1.  **建日曆表（build_calendar）**：從 2023-01-01 到 2026-05-31 每天一列，逐一標上：星期幾、月份、是否週末、是否國定假日（用政府辦公日曆的放假清單）、是否補班（週六上班日，8 天）、是否春節（除夕\~初三）、是否颱風停班（6 天，含颱風名）、是否跨年夜。其中 `is_offday`（實際放假）的邏輯值得看：「(週末 且 非補班) 或 國定假日」——補班的週六雖是週六但要上班，所以不算放假日。這種「制度細節」寫錯，模型就會在 8 個補班日全部預測錯方向。
2.  **算站點靜態特徵（build_static）**：只用 2023-01～2024-12 的資料（防洩漏），算每站的 PageRank、進出流量強度、連接站數，並做上面 Stage 2 說的分群。**PageRank 的白話原理**：想像每個站有一批「聲望值」，每輪把自己的聲望按出站流量比例分給下游站，反覆兩百輪後穩定下來的分數就是 PageRank——被很多「本身也重要的站」大量送客的站，分數高。它衡量的是「路網中的樞紐地位」，跟單純的進站量不同。
3.  **建完整網格再填零（build_feature_table）**：先造出「119 站 × 1,247 天」的完整表格（148,393 列），再把實際進站量填進去，**缺的格子填 0 而不是刪掉**。為什麼？颱風停班日某些小站真的一個人都沒有，原始明細裡就沒有那一列——但「0 人進站」是真實發生的事實，不是缺資料。若直接刪掉，模型永遠學不到「颱風日=沒人」。
4.  **算動量特徵（先 shift 再 rolling）**：lag1＝昨天、lag7＝上週同日、roll7_mean＝「昨天以前 7 天」的平均。關鍵是所有計算前先 `shift(1)` 把序列往後推一天。
5.  **收尾自動驗證（validate）**：斷言檢查——每站列數相同（網格完整）、颱風日恰好 6 天、補班日恰好 8 天，並印出「颱風日運量只有平日約兩成」這種常識檢核。特徵表做完不是直接用，是先讓它自證沒做壞。

**小例子（shift(1) 到底防了什麼）**：假設動物園站的進站量是——

| 日期   | 3/8   | 3/9   | 3/10  | 3/11  | 3/12  | 3/13        | 3/14        | 3/15(要預測) |
|--------|--------|--------|--------|--------|--------|--------|--------|--------|
| 進站量 | 5,000 | 5,200 | 4,800 | 5,100 | 5,300 | 9,000(週六) | 8,500(週日) | ？           |

預測 3/15 時，正確的 roll7_mean 是 3/8\~3/14 的平均（6,129）。如果忘了 shift(1)，rolling 視窗會變成 3/9\~3/15——把「3/15 的答案」偷偷算進特徵裡。模型成績會漂亮得不真實，上線後立刻打回原形。這就是資料洩漏：**測驗成績好不是因為會，是因為考卷夾了答案。**

### Stage 5：基準線 + 統計模型（已完成，成績見第 7 節）

**做什麼**：先立標尺（笨方法基準線），再派出統計世代的正式選手 SARIMAX。

**四個基準線的白話邏輯**：

-   naive：「明天 = 今天」。WAPE 13.44%。
-   seasonal naive：「明天 = 上週同一天」。WAPE 9.88%——比 naive 好很多，證明**週節律**是這份資料最強的規律。
-   rolling28：「明天 = 最近28天平均」。WAPE 15.83%——最差，因為平均把週末平日抹平了。
-   逐站 OLS 線性迴歸：用星期幾+假日旗標+動量線索做最簡單的線性組合。WAPE 4.83%——樸素但強悍。

**SARIMAX 的白話邏輯**：對每一站各建一個模型（共 119 個），骨架是「每 7 天一個循環」的週期結構，再外掛假日/春節/跨年/補班四個旗標。測試期採用「參數凍結、逐日滾動」的方式做真正的一步預測。成績 **WAPE 4.34%**，119 站全部收斂——**這是全專案除了 LSTM 之外最好的成績，贏過兩個樹模型。**

> **⚠️ 這個數字修正過，而且是本專案最重要的一次除錯。**
>
> 原本記錄的是 6.07%。問題出在測試期做 filtering 時，程式只餵了訓練集最後 14 天當「暖身」就開始預測。但 statsmodels 的狀態空間模型遇到這種情況會重新初始化 Kalman filter（diffuse initialization），而 `(1,1,1,7)` 的季節差分先吃掉 7 個觀測，只剩約 7 個點根本不夠讓狀態收斂。
>
> **後果**：測試期前 7 日的 WAPE 是 **17.50%**（修正後 5.90%），而且污染一路延續——第 15 日之後仍差 0.88pp。整體被高估了 **1.73pp** 的誤差。
>
> **正確做法**是用 `.extend()` 延續訓練期結束時的狀態（實測與「`.apply()` 餵完整序列」逐站結果完全相同，最大差異 0.0），這也才符合報告自己聲明的「預測第 t 日時可用 t−1 之前**所有**真實觀測」協定。
>
> **教訓**：一個看似無害的「只帶 14 天暖身」寫法，足以扭轉整個世代比較的結論——若沒發現，本專案會得出「統計方法明顯不如 ML」的錯誤結論。**評估基準線時，實作正確性的風險與模型選擇的風險同量級。**

**重現**：

```         
pip install statsmodels tqdm
python scripts/stage5_baseline.py            # 全部119站，約3-30分鐘（視機器）
python scripts/stage5_baseline.py --limit 5  # 先跑5站確認環境
```

產出 `reports/stage5_baseline_metrics.csv`（基準線成績）、`stage5_sarima_metrics.csv`（SARIMAX 逐站成績）、`stage5_sarima_predictions.csv`（逐日預測值，留給 Stage 8 做事件分析）。報告在 `reports/stage5_baseline_report.md`。

**▍具體邏輯與步驟**

1.  **三個笨基準線根本不用「訓練」**：naive 的預測值就是特徵表裡的 lag1 欄、seasonal naive 就是 lag7 欄、rolling28 就是 roll28_mean 欄——直接拿來跟實際值算誤差即可。基準線便宜到不做就是懶惰。
2.  **逐站 OLS 的設計**：對每一站解一條線性迴歸——特徵是「星期幾（6 個 0/1 變數）＋4 個事件旗標＋5 個動量特徵」，用訓練期資料求出係數，再套用到測試期。白話說，OLS 學的是「這站的週一通常是多少人、放假日打幾折、昨天的量要參考幾成」這種加權配方，每站一套配方共 119 套。
3.  **SARIMAX 的「參數凍結、狀態滾動」**：訓練期估好參數後，測試期**不重新估參數**，而是逐日把新觀測「餵」給模型更新其內部狀態（filtering），再預測下一天。打個比方：導航的路線規則（參數）出發前就定好，但你目前的位置（狀態）隨時更新——每天的預測都用到昨天的真實值，卻沒有偷改規則。這才是真正的一步預測；若逐日重估參數，等於每天重新訓練 119 個模型，運算量爆炸且比較基準就變了。
4.  **預測值一律 clip 到 ≥0**：模型偶爾會吐出負的預測人數，物理上不可能，直接截為 0。

**小例子（WAPE 怎麼算）**：假設某兩天兩個站——大站實際 100,000 人、預測 96,000（差 4,000）；小站實際 500 人、預測 800（差 300）。WAPE＝(4,000+300)÷(100,000+500)＝4.3%。若用 MAPE（逐筆算百分比再平均）＝(4%+60%)÷2＝32%——小站那筆 60% 把整體炸了，但 300 人的誤差對捷運調度根本無關緊要。這就是本專案選 WAPE 的理由。

### Stage 6：機器學習（已完成，成績見第 7 節）

**做什麼**：改用樹模型。與 Stage 5「一站一個模型」不同，這裡訓練**一個全站共用的 global model**——不告訴模型站名，只告訴它站的「個性」（分群+靜態特徵），逼模型學「站型的規律」而不是「背 119 個站的答案」。這個設計直接服務研究問題：如果模型靠站型就能預測得好，就證明「按站型配置工具」可行。

**調參紀律**：所有超參數只用 2025 驗證段挑選，挑好後用 train+val 重訓，測試段只評一次（挑選過程留檔於 `stage6_tuning_log.csv`，備查）。

**結果一：一個模型打贏 119 個逐站 OLS，但輸給 119 個逐站 SARIMAX**。LightGBM 單一 global model 的 WAPE 4.63%，勝過 119 個逐站 OLS（4.83%），但**落後修正後的 SARIMAX（4.34%）0.29pp**。輔助模型隨機森林 4.62%，與 LightGBM 幾乎打平（差 0.01pp，屬雜訊等級）——兩個原理不同的樹模型成績一樣，說明**成績是特徵決定的，不是模型形式**。

> **這一條在 2026-08-06 反轉了。** 原本寫的是「一個 global model 打贏 119 個逐站模型」，那是建立在 SARIMAX 6.07% 這個有缺陷的數字上。修正後古典統計方法在整體指標上勝出，**「更複雜的模型 = 更好的預測」在本資料上不成立**。global model 的價值改由**維運成本**（1 套 vs 119 套）、**新站納入**（只需靜態特徵即可預測）與**特徵擴充性**（樹模型可直接吸納場站級事件特徵，SARIMAX 的 exog 機制受限）論證，而非整體精度。

**結果二：消融實驗證明路網資訊有用**。把網絡特徵（PageRank、流量強度、度數）拿掉再跑一次，WAPE 從 4.634% 退到 4.996%（+0.362 個百分點，相對變差約 7%），其中**就業極端型退最多（+0.663pp，是住宅型 0.188pp 的 3.5 倍）**。白話解釋：global model 不知道站名，網絡特徵就是它辨識「這是個什麼量級、什麼位置的站」的身分證——那 7 站量體只占 3.6%、曲線又極端，模型光靠日曆與動量認不出它們是誰。特徵重要度中 **PageRank 依 gain 排第 1（占 45.31%）**。

> **重要度基準也修正過**：`LGBMRegressor.feature_importances_` 預設回傳的是 **split（分裂次數）而非 gain**，原報告誤標為 gain。依 split，PageRank 只排第 8、網絡特徵合計占 14.2%——這與「拿掉它們會退 0.362pp」自相矛盾。改用 gain 後 PageRank 躍居第 1、網絡特徵合計 45.7%，才與消融結果自洽。**兩種基準會給出完全不同的故事，引用時必須註明用的是哪一種。**

**結果三（最重要的發現）：觀光站的問題不是模型不夠強，是資訊不夠**。ML 對 OLS 的改善幅度按站型看：就業極端型 -1.08pp、就業 -0.33pp、住宅 -0.24pp、**觀光型 +0.05pp（反而變差）**——換了更強的樹模型，觀光站不進反退。觀光站缺的是「場站級事件」線索（演唱會、展覽、天氣），目前特徵裡只有全市級的日曆旗標。這直接指出 Stage 8 事件分析的方向。

> **但這個「天花板」後來被 LSTM 打破了**（4.55%，見 Stage 7）。Stage 6 當時觀察到三種典範全部卡在 5.02–5.21% 的窄帶，曾據此推論「瓶頸在資料而非模型」——**這個推論形式上有缺陷**：「多種方法都做不到」只能證明「已嘗試的方法都做不到」，無論試幾種都無法排除下一個典範。正確的表述是「**樹模型**對觀光型無增益」，而非「觀光型不可再改善」。

**重現**：

```         
pip install scikit-learn lightgbm
python scripts/stage6_ml.py --quick   # 縮小網格快速驗證環境
python scripts/stage6_ml.py           # 完整版
```

產出 `reports/stage6_ml_report.md`（完整報告）、`stage6_ml_metrics.csv`、`stage6_ml_predictions.csv`（LightGBM 逐日預測，供 Stage 8）、`stage6_tuning_log.csv`、`stage6_feature_importance.csv`。

**▍具體邏輯與步驟**

1.  **資料準備**：讀特徵表、丟掉每站前 28 天（lag/rolling 是 NaN）、把站型標籤做 one-hot（residential/employment/tourism_hub 各變成一個 0/1 欄）——樹模型只吃數字，不吃文字類別。
2.  **梯度提升樹的白話原理**：第一棵樹粗略預測，第二棵樹專門學「第一棵哪裡錯了」，第三棵學前兩棵合計還錯哪裡……幾百棵小樹接力修正，每棵只修一小步（學習率 0.05 = 每步只走 5%），最後加總。RandomForest 則是幾百棵「各自獨立、各看資料的隨機子集」的樹投票平均。兩者原理不同，成績卻打平——這正是「成績由特徵決定」的證據。
3.  **調參的紀律化流程**：對每組候選超參數（葉子數、最小葉樣本數、學習率、樹的數量），用 train（2023–2024）訓練、看 validation（2025）的 WAPE，全部記進 `tuning_log.csv`；選出 val 最好的一組後，**用 train+val 合併重訓一次**（讓模型多學 2025 年的資料），再對 test 評「唯一的一次」。test 從頭到尾只被碰這一下。
4.  **消融實驗的做法**：把 5 個網絡特徵欄（pagerank、進出流量強度、進出度數）整組拿掉，**重新走一遍完整的調參流程**再評 test——不是拿舊模型直接砍特徵。因為少了特徵後最佳超參數可能不同，不重新調參的消融是不公平的比較。

**小例子（「不給站名」是什麼意思）**：模型看到的一列資料長這樣——「lag7=5,200、pagerank=0.021、就業型=1、假日=0……→ 進站量 6,100」。它不知道這是「西門站」，只知道「一個上週同日 5,200 人、樞紐程度中上的就業型站」。於是它學到的規律是「這種站放假日大概掉三成」，而不是「西門站放假日掉三成」——前者能泛化到任何同型的站（包括未來的新站），後者只是背答案。

### Stage 7：深度學習（LSTM）（已完成，成績見第 7 節）

**做什麼**：換深度學習世代的選手上場。LSTM 直接「閱讀」每站最近 28 天的原始序列（進站量＋假日、停駛旗標共 3 條通道），搭配 25 維的日曆與車站靜態線索，一樣是全站共用一個 global model。站的身分改用 **station embedding** 表達：讓模型自己為 119 個站各學出一組 12 維的數字代號——功能類似 Stage 6 的「站型+網絡特徵」身分證，但由模型自動學習而非人工給定。

**調參紀律（與 Stage 6 完全相同）**：三組架構只用 2025 驗證段比較（early stopping 早停），由 val WAPE 選出 hidden=128 單層（3.958%，勝過 64×2層的 3.978% 與 64×1層的 3.989%）；再用 train+val 重訓 24 個 epoch（即 val 選出的最佳輪數），測試段只評一次。全程 CPU 訓練約 15–20 分鐘（依實測 log 累計），固定 random seed 可重現。

**結果：LSTM WAPE 3.99%，全場冠軍——但只贏了 54/119 站**。按站型：住宅 3.29%、就業 3.97%、就業極端 3.60%、觀光 4.55%。

**兩個方向相反的發現，合起來才是完整的故事：**

**（一）LSTM 打破了 Stage 6 推論的「觀光型天花板」**。觀光轉運型從最佳的 SARIMAX 5.02% 推進到 **4.55%**，而且是在**沒有加入任何場站級事件特徵**的情況下。可能的解釋：樹模型只拿到 5 個人工摘要的動量特徵（lag1/7/14、兩個移動平均），LSTM 讀的是完整 28 天序列，承載了摘要之外的動態資訊（例如活動前後的連續爬升與衰退曲線）。

**（二）但 LSTM 在住宅型與就業極端型輸給 SARIMAX**（3.29 vs 3.23、3.60 vs 3.17），而且只在 **54/119 站**優於 SARIMAX——它之所以贏得整體指標，是因為那 54 站占了 **66.2% 的總運量**。按站點大小拆開看非常清楚：

| 運量四分位       | LSTM 勝出站數 | 平均 LSTM | 平均 SARIMAX |
|------------------|---------------|-----------|--------------|
| Q4（最大 30 站） | **25 / 30**   | 4.23%     | 4.85%        |
| Q3               | 16 / 29       | 3.48%     | 3.49%        |
| Q2               | 5 / 30        | 4.00%     | 3.93%        |
| Q1（最小 30 站） | **8 / 30**    | 5.41%     | 4.84%        |

**就業極端型更是 0/7 站勝出，全軍覆沒——但這不是量體造成的。** 這7站量體橫跨 Q1\~Q4：松江南京日均近6萬、全站排名第15（Q4大站），一樣輸給SARIMAX（2.90% vs 2.70%）；橋和日均僅1,645（Q1倒數第2）也輸（6.43% vs 4.03%）。真正的共同點是**模式規律性**：這群站進站量幾乎全壓在單一晚間尖峰、近乎決定性、雜訊極低。逐站SARIMAX能為單一站精準擬合季節性結構，不受其他118站干擾；Global LSTM因為要用同一套權重服務119站，容量是為了處理多數站的複雜多因素模式而磨合出來的，套用在這種單峰極端規律模式上反而是負擔——即使有station embedding提供站點身分，那也只是共用轉換邏輯的輸入條件，不是讓模型為該站切換出一套獨立處理機制。量體軸（大站利Global、小站利per-station）與規律性軸（規律利per-station、複雜利Global）是兩個獨立因素，就業極端型剛好把兩者分開驗證。

改善與退步的名單也分得極乾淨——改善最多的是國父紀念館（-3.05pp）、台北 101（-1.84pp）、輔大、圓山，**全是大型活動／展演場站**；退步最多的是中原（+2.71pp）、新埔民生、橋和，這幾站量體確實都小（Q1）。機制一致：station embedding 加序列建模讓模型能學站點特有的不規則行為，但這種彈性對本來就規則的站（不論量體大小）是負擔。

**「最複雜的模型整體最好」與「最複雜的模型處處最好」是兩回事**——四世代對照證明了前者成立、後者不成立。這個區分是最終治理建議的核心。

**重現**：

```         
pip install torch
python scripts/stage7_dl.py    # CPU 可跑，全程約 15–20 分鐘
```

產出 `reports/stage7_dl_report.md`（完整報告）、`stage7_dl_metrics.csv`（成績）、`stage7_dl_predictions.csv`（逐日預測，供 Stage 8）、`stage7_training_log.csv`（調參過程備查）。

**▍具體邏輯與步驟**

1.  **把表格變成「一個個訓練樣本」**：對每個「站 × 目標日」，切出目標日前 28 天的序列當輸入、目標日的進站量當答案。一個樣本＝（28 天的歷史窗，今天的情境，站的編號）→（今天的量）。訓練集共 83,657 個這樣的窗。
2.  **每站各自 z-score 標準化**：台北車站日均 30 萬、小站 3 千，直接餵原始數字，大站的誤差會淹沒一切。所以每站用**自己 train 期**的均值和標準差把量轉成「偏離自己正常水準幾個標準差」——z=0 是正常日、z=+3 是爆量日。兩個防洩漏細節：統計量只用 train 期算（不讓 2025/26 的水準洩漏進來）；標準差下限設 1（避免流量近乎不變的停駛站除出天文數字）。
3.  **三路輸入的分工**：LSTM 讀 28 天序列（負責「最近的動態」——連漲三天？上週有異常？）；25 維情境向量（負責「今天是什麼日子」——星期、月份、假日、颱風等旗標）；12 維 station embedding（負責「我是誰」——每站一組可學習的數字代號，訓練中自動調整，功能上等於模型自己學出來的站型特徵）。三路拼接後過一層小型神經網絡輸出預測的 z 值，再乘回該站的標準差、加回均值，還原成人數。
4.  **早停（early stopping）的邏輯**：每練完一輪（epoch）就在 validation 上量一次 WAPE；連續幾輪不再進步就停，記下「第幾輪最好」。三種架構各自早停後比 val 成績，選出勝者（hidden=128 單層，第 24 輪最好）。
5.  **重訓的固定輪數**：最終用 train+val 合併重訓時，**沒有 val 可以看了**（val 已併入訓練），所以直接練「剛剛記下的 24 輪」就停。這是「調參用 val、考試只考一次」紀律在深度學習的具體實現。

**小例子（z-score 讓大小站說同一種語言）**：颱風天，台北車站從 30 萬掉到 6 萬、小站從 3,000 掉到 600——原始數字看是「掉 24 萬」vs「掉 2,400」，天差地遠；z-score 看都是「掉到 -3 個標準差」，是同一件事。模型因此能把「颱風的形狀」從所有站的經驗中一起學會，而不是 119 個站各學各的。

### Stage 8：SHAP 可解釋性 + 事件分析（已完成）

**做什麼**：三件事——(A) 用 SHAP 解釋 LightGBM「靠什麼預測」；(B) 三世代模型在 test 期各種日型的誤差對照；(C) hold-out 事件實驗：把模型訓練「凍結在事件發生前一天」，模擬事件前夕做預測。

**結果一：不同站型依賴不同特徵（研究問題的機制面證據）**。SHAP 佔比顯示：住宅型最依賴「上週同一天」（lag7 佔 34.5%，全場最高）——週節律規則，所以逐站 SARIMAX 就足以勝過所有 ML/DL；**觀光型最依賴網絡位置（PageRank 佔 19.9%、網絡特徵合計 26.4%，皆為全場最高），且週節律最弱（lag7 僅 27.1%）**——它的規律性差，模型只能改用「這是哪一種站」的靜態身分來錨定水準；就業型反而最不依賴網絡特徵（合計 13.0%），因為站數多、行為典型，動量特徵已足夠。而**事件旗標在所有站型的佔比都不到 1%**（四個稀有旗標合計僅 0.74%）——春節、跨年這種日子在訓練資料裡太稀有，模型結構上學不重它們。

> **一個容易誤讀的地方：SHAP 佔比 ≠ 效能貢獻。** 觀光型的網絡特徵佔比最高（26.4%），但 Stage 6 消融顯示移除網絡特徵對觀光型的損害（0.410pp）反而**低於**就業極端型（0.663pp）。原因是 SHAP 衡量「模型輸出隨該特徵變動的幅度」，消融衡量「移除後無可替代的資訊損失」——觀光型大量使用 PageRank 來定位水準，但這個資訊可被部分替代；就業極端型使用得少，卻無可替代。**「重要度高」不等於「不可取代」**，這個區分在可解釋性應用中經常被混淆。

**結果二：極端節日出現明顯的世代反轉**。平常日子是 LSTM 最好（平日 2.79%、週末 5.61%、國定假日 8.45%），但**極端節日 SARIMAX 大幅領先**：

| 日型               | 天數 | SARIMAX    | LightGBM | LSTM      |
|--------------------|------|------------|----------|-----------|
| 一般平日           | 97   | 2.98%      | 3.13%    | **2.79%** |
| 一般週末           | 44   | 6.59%      | 6.72%    | **5.61%** |
| 其他國定假日       | 5    | 11.10%     | 10.43%   | **8.45%** |
| 春節（除夕\~初三） | 4    | **15.35%** | 20.52%   | 20.32%    |
| 元旦（跨年翌日）   | 1    | **12.39%** | 28.27%   | 16.15%    |

**為什麼會反轉？關鍵在 global model 與逐站模型對「稀有事件」的處理方式完全不同**（[global model／逐站模型](#2-先備知識小字典)定義見第 2 節）：

-   **逐站模型（SARIMAX，119 個獨立模型）**：`is_lny`、`is_nye` 的係數是**只用這一站的歷史**估計出來的，不必跟其他 118 站的資料「競爭」。即使這一站的春節樣本本身也不多，模型的全部注意力都只服務這一站，估得出、也用得上一個屬於這一站的「春節效應」。
-   **Global model（LightGBM／LSTM，1 個模型服務 119 站）**：一套參數／權重要同時擬合 119 站×上千天的資料，優化方向是「對訓練集裡絕大多數樣本最好」。春節每年只有 4 天、跨年只有 1 天，訓練資料（train+val）合計約 12.7 萬列裡稀有事件旗標只占極小比例——對 LightGBM，這種特徵幾乎不會被拿去做關鍵分裂（SHAP 證實四個稀有旗標合計貢獻僅 0.74%）；對 LSTM，這種日子的訓練訊號同樣會被其餘 99% 以上「一般日子」的訊號稀釋掉。**稀有事件在 global model 眼中統計上太不顯著，不足以撐起一條專屬的判斷路徑。**

一句話：**「global model 優於逐站模型」在稀有事件上完全不成立**——逐站模型的客製化，正是稀有事件唯一需要的東西。

> 註：原版此處記載「元旦 SARIMAX 崩潰 48.3%」，那是 Kalman filter 缺陷造成的假象（測試期首日正是 2026-01-01，暖身不足傷害最重之處）。修正後 SARIMAX 在元旦為 12.39%、**反而是三世代最佳**，論述方向完全反轉。

> **這張表容易被誤讀成「LSTM 全面比 SARIMAX 強」，其實只是全站平均後的結果。** 拆到站型會發現，即使是這張表計入的「平日」等一般日子，住宅型與就業極端型的**整體**（含平日在內全部 151 天）WAPE 仍是 SARIMAX 贏（3.29% vs 3.23%、3.60% vs 3.17%，見 Stage 7）。原因跟稀有事件是同一套邏輯：這兩型站的日常規律極端乾淨（住宅型早尖峰通勤、就業極端型單一晚尖峰，幾乎決定性、雜訊很低），逐站 SARIMAX 能把全部參數都貼著這一站的規律去擬合；LSTM 的 **station embedding 只是餵給共用轉換邏輯的一個輸入條件（告訴模型「我是誰」），不是讓模型為這一站切換出一套獨立的處理機制**——那套轉換邏輯的容量，是為了同時處理 119 站裡多數站更複雜、更吵雜的模式而磨合出來的，套用在這種極端規律的少數站型上反而是負擔，就算加了 embedding 也補不回這個落差。整張表的 2.79% 之所以仍是 LSTM 贏，是因為觀光型、大型站這類 LSTM 佔優勢的站型體量夠大，蓋過了規律站型的小幅落後。

**結果三：事件旗標有效的前提是「經歷過」**。颱風 hold-out 實驗：模型第一次遇到颱風（卡努）時，旗標完全無效（挽回 +0.0pp）——樹模型對訓練期沒見過的旗標值無法反應；看過 1\~3 次後，旗標挽回 +199\~775 個百分點。而完全沒有特徵可用的新北耶誕城開幕，模型連續三天系統性低估 6.8\~7.7%——這就是「觀光站缺場站級事件資訊」的直接量化。

**治理轉譯**：平常日按站型配置工具就夠；颱風、春節等極端日不要依賴任何模型的點預測；最便宜的改進不是換更強的模型，而是接入活動日曆資料。

**重現**：`pip install shap` 後 `python scripts/stage8_shap_events.py`。產出 `reports/stage8_shap_events.md`（完整報告）、`stage8_*.csv` ×4、圖 6\~10。

**▍具體邏輯與步驟**

1.  **SHAP 的白話原理**：把一筆預測想成「全隊合作的成果」，SHAP 回答「每個特徵貢獻了幾分」。算法上源自賽局理論的 Shapley 值：輪流把某特徵「加入/移出」各種特徵組合，看預測平均變動多少，就是它的公平貢獻。樹模型有精確又快的算法（TreeSHAP），所以作用在 LightGBM 上。腳本先按 Stage 6 的最佳參數重訓模型，並先確認 test WAPE 還原到 4.62%（sanity check：解釋的必須是「同一個」模型）。
2.  **三個層次的 SHAP**：全域（所有預測的貢獻絕對值取平均——模型整體最依賴什麼）；按站型（同上但分組，且因三站型量級不同改用「佔比」比較——就業站的貢獻天然是大數字，直接比絕對值不公平）；單筆（挑除夕的台北車站這種具體日子，畫出「哪些特徵把預測推高／拉低了幾人」的瀑布）。
3.  **日型誤差對照的做法**：把 test 期 151 天分成五種日型（平日/週末/國定假日/春節/元旦），分別對三個世代的預測檔（stage5/6/7 都留了逐日預測 csv，就是為了這一刻）各算 WAPE——「誰在哪種日子輸」一目了然。
4.  **Hold-out 事件實驗的關鍵設計**：想知道「颱風旗標有沒有用」，不能拿看過該颱風的模型來測。做法：把訓練資料**嚴格截到事件首日的前一天**（例如測凱米就只用 2024-07-23 以前的資料訓練），然後兩個版本各訓一次——有 is_typhoon 旗標 vs 拿掉旗標——比較事件日誤差，同場加映 seasonal naive 當笨基準。四個颱風按時間排開，剛好排成「模型此前經歷過 0/1/2/3 次颱風」的對照序列。
5.  **知道哪個實驗不用做**：0403 地震「不做」對照實驗——停駛旗標在事件前全是 0，樹模型對沒見過的旗標值切不出任何分支，有無旗標的預測**必然**一模一樣，實驗零資訊量。把「為什麼不做」的結構原因寫進報告，比跑一個空實驗更專業。

**小例子（zero-shot 為什麼註定失敗）**：訓練資料裡 is_typhoon 永遠是 0，樹在建構時問「is_typhoon 是 0 還是 1？」發現所有樣本都是 0——這個問題毫無區分力，樹根本不會用它。第一次真的來颱風（旗標=1），模型走的路徑跟平常日完全相同，預測自然是「正常的一天」。等它「經歷」過一次颱風（訓練資料裡出現過旗標=1 且當天量暴跌），樹才學得到「旗標=1 → 砍到剩兩成」的分支。這解釋了實驗結果：卡努（第 0 次）挽回 +0.0pp，凱米之後挽回 +199\~775pp——**模型的應變能力來自經歷，不來自旗標本身。**

> **為什麼 hold-out 只用 LightGBM，不用同為 global model 的 LSTM？**
>
> 1.  **成本**：hold-out 是「每個事件各切一次、各訓兩版」，光颱風就是 4 事件 × 2 版本 = 8 次重訓，加上跨年、耶誕城更多。LightGBM 一次數分鐘，LSTM 一次要跑 24 epochs（CPU 約 15–20 分鐘），在單機上是量級差異。
> 2.  **機制演示的乾淨度**：研究問題 (b) 要揭露的核心機制是「樹模型對訓練期未見的旗標值無法建立切分點」，而卡努那組有旗標與無旗標的預測**完全相同**（294.6 vs 294.6）——這是無可爭辯的證明。LSTM 沒有這個限制：旗標是連續輸入乘進權重矩陣，即使訓練期從未出現 1，輸入 1 仍會產生不同的激活值，預測會不同（只是不會準）。換成 LSTM，zero-shot 的結果會變成「有差但差得不多」，訊號模糊，那個乾淨的故事就講不出來。**用樹模型是為了讓機制看得見。**
> 3.  **與 SHAP 分析共用同一個模型**：本階段 (A) 的 SHAP 拆解對象是 LightGBM，(C) 沿用同一個模型，機制解釋才前後銜接得起來（例如 local SHAP 觀察到跨年夜污染 lag1，正好對應 hold-out 的跨年結果）。換模型會斷掉這條敘事線。
>
> **但這是一個需要主動聲明的範圍限制。** LSTM 是全篇最佳模型（3.99% vs LightGBM 4.63%），且在五個事件型場站中的四站最佳——這暗示序列模型對事件日的處理機制與樹模型不同：LightGBM 只看得到 lag1/lag7/lag14 與移動平均，事件當天長什麼樣完全靠旗標告訴它；LSTM 讀的是近 28 天的原始序列，颱風前一天若已有提前反應（民眾預先採買、公司提前放人），LSTM 可能從序列本身就讀到訊號，對旗標的依賴天生較低。
>
> 因此「事件旗標挽回大部分誤差」這個結論**嚴格說只對樹模型成立**，不能推廣成「事件特徵對所有模型都有這麼大價值」。若要補強，最低成本的做法是只挑康芮一場跑 LSTM 有／無旗標對照（2 次重訓）當 robustness check；若 LSTM 的相對降幅明顯較低，反而會是更有份量的發現——**模型架構決定了事件特徵的邊際價值。**


### Stage 9：整合（已完成）

README、requirements.txt、.gitignore 已備；最終作品集報告 `MRT作品集報告.docx`（13 頁，摘要→動機→資料工程→EDA→特徵與協定→三世代方法→結果→SHAP→事件→治理洞見→限制→重現性，含 8 圖 7 表）已完成。剩 Portfolio 網頁由作者更新。

------------------------------------------------------------------------

## 6. 三條防洩漏鐵律（本專案最重要的設計）

「洩漏」= 模型偷看了預測當下不可能知道的資訊。本專案的三條鐵律：

1.  **所有 lag/rolling 特徵先 shift(1) 再計算**。預測 3/15 時，「最近 7 天平均」只包含 3/8～3/14，絕不含 3/15 當天。
2.  **車站靜態特徵（分群、PageRank、流量強度）只用 2023–2024 訓練期的資料計算**。如果用全期資料算，等於讓 2026 年的資訊滲進特徵，測試就不公平了。就算有人說「用全期算比較準」，也要拒絕——公平比較優先。
3.  **事件旗標用「事前可得」的官方公告**。颱風停班是前一晚人事行政總處公布的（預測明天時已經知道），不是事後看運量掉了才反推標記。

這三條在口試會被問「你怎麼確定模型沒作弊」，答案就在這裡。

------------------------------------------------------------------------

## 7. 目前成績表與怎麼讀

**主表**（test = 2026/01–05、一步預測、WAPE，越低越好）：

| 模型 | 世代 | 整體 | 住宅型 | 就業型 | 就業極端型 | 觀光型 |
|-----------|-----------|-----------|-----------|-----------|-----------|-----------|
| rolling28（抄月均） | 基準 | 15.83% | 15.36% | 16.34% | 35.80% | 13.63% |
| naive（抄昨天） | 基準 | 13.44% | 13.45% | 13.75% | 25.07% | 11.95% |
| seasonal naive（抄上週） | 基準 | 9.88% | 8.70% | 10.45% | 13.23% | 9.84% |
| 逐站 OLS | 統計 | 4.83% | 4.17% | 4.95% | 5.34% | 5.15% |
| LightGBM global | ML | 4.63% | 3.93% | **4.62%** | 4.26% | 5.20% |
| RandomForest global | ML | 4.62% | 3.72% | 4.71% | 4.45% | 5.21% |
| SARIMAX 逐站 | 統計 | 4.34% | **3.23%** | **4.62%** | **3.17%** | 5.02% |
| **LSTM global** | DL | **3.99%** | 3.29% | **3.97%** | 3.60% | **4.55%** |

**怎麼讀出研究故事**：

1.  **每一列都要贏過上面的基準線才有存在價值**。seasonal naive 9.88% 是及格線；整體冠軍是 LSTM 3.99%。但**注意排序**：修正後的 SARIMAX（4.34%）贏過兩個樹模型（4.62–4.63%），統計世代並沒有被 ML 世代取代。
2.  **橫著讀才是研究問題的答案，而且沒有任何一個模型處處最好**：
    -   **住宅型與就業極端型**：SARIMAX 最佳（3.23%、3.17%），連 LSTM 都輸。這兩型的通勤節律最規則，週季節差分 `(1,1,1,7)` 正好對症——**模型與資料結構的匹配度勝過模型容量**。
    -   **就業型**：SARIMAX 與 LightGBM 完全打平（同為 4.62%），LSTM 領先（3.97%）。
    -   **觀光型**：三個世代全部卡在 5.02–5.21%，唯有 LSTM 突破到 4.55%。這型的規律性最差，需要的是序列表達力——或者更根本地，需要場站級事件特徵。
    -   **就業極端型的跨世代跨度最大**：35.80%（rolling28）→ 3.17%（SARIMAX），**11.3 倍**。同一群站，難度完全由「方法能否表達星期節律」決定，不是站點的固有屬性。
3.  **最終的治理結論是分層策略，每一層都有實證**：小型高規律站（住宅、就業極端）用逐站 SARIMAX；大型與事件型站（觀光、大型就業）用 global LSTM。理由不只是整體分數，而是 Stage 7 逐站拆解顯示 LSTM 只在 54/119 站勝出、且集中於大站（Q4 勝率 25/30、Q1 僅 8/30）。

> **這張表在 2026-08-06 大幅變動**，主要來自 SARIMAX 的實作修正（6.07% → 4.34%）。原版的敘事是「世代越新越好、ML 打贏逐站模型、觀光型是資料天花板」，三句都已不成立。**新版的故事更難被挑戰**：它不是「我試了越來越複雜的模型然後最複雜的贏了」，而是「我用嚴謹的對照證明了複雜度在此問題上的邊界在哪裡」。 3. **誤差最大的站透露下一步**。SARIMAX 表現最差的五站——國父紀念館 16.1%、動物園 15.3%、南港展覽館 12.1%、圓山 11.6%、淡水 10.8%——全是大型活動、展覽、天氣敏感的場站，與第 2 點互相印證。補上「場站級事件特徵」能挽回多少，就是 Stage 8 要回答的。 4. **消融實驗（拿掉路網特徵再跑）**：WAPE 4.62%→5.01%，證明 OD 路網資訊在「進站量」之外提供了增量價值——這回答了次要研究問題(a)。

**逐站/逐日成績去哪看**：`reports/stage5_sarima_metrics.csv` 有 SARIMAX 119 站逐站成績；`stage5_sarima_predictions.csv`、`stage6_ml_predictions.csv`、`stage7_dl_predictions.csv` 有每站每天的「實際 vs 預測」，想畫任何一站的預測曲線都從這裡取材；`stage6_ml_metrics.csv` 與 `stage7_dl_metrics.csv` 有 ML/DL 模型全部指標。

------------------------------------------------------------------------

## 8. 從零重現整個專案（Quickstart）

```         
# 0. 環境：Windows、Python 3.10+，於 F:\Project_MRT 執行
pip install duckdb pandas numpy matplotlib scikit-learn statsmodels tqdm lightgbm torch

# 1. 下載資料（約數小時，可中斷續跑；已有 data/parquet 者跳過）
python scripts/stage1b_download_od.py

# 2. EDA：產出報告、圖1~5、聚合表
python scripts/stage2_eda.py

# 3. 特徵工程：產出 feature_table.csv
python scripts/stage4_features.py

# 4. 基準線 + SARIMAX（約3-30分鐘）
python scripts/stage5_baseline.py

# 5. 機器學習 + 消融實驗
python scripts/stage6_ml.py

# 6. 深度學習 LSTM（CPU 約 15–20 分鐘）
python scripts/stage7_dl.py

# 7. SHAP 可解釋性 + 事件分析（需先 pip install shap）
python scripts/stage8_shap_events.py

# （選用）環狀線移交斷點診斷：重現第3節的斷點量化表，需先跑完步驟2
python scripts/stage2b_ring_handover_diagnostic.py
```

每一步的產出都會落在 `reports/` 或 `data/`，與第 5 節逐段對應。順序不可顛倒（後面的腳本依賴前面的產出）。

------------------------------------------------------------------------

## 9. 檔案結構速查

```         
F:\Project_MRT\
├── PROJECT_GUIDE.md                ← 本檔（入門指南）
├── README.md                       ← GitHub門面（結果摘要+重現步驟）
├── requirements.txt / .gitignore
├── MRT_project_handoff.md          ← 完整交接文件（給AI協作者/進階讀者）
├── MRT_project_ai_brief.md         ← 簡版進度brief
├── scripts\                        ← 所有可重現腳本（stage0~8）
├── data\
│   ├── parquet\od\                 ← 41個月原始資料（parquet，207MB）
│   ├── agg\                        ← EDA聚合表×6
│   └── features\                   ← feature_table.csv + 資料字典
└── reports\
    ├── stage2_eda_report.md ＋ figures\fig1~5.png
    ├── stage2b_ring_handover.csv   ← 環狀線移交斷點量化（第3節那張表）
    ├── stage5_baseline_report.md   ← 基準線+SARIMAX完整報告
    ├── stage6_ml_report.md         ← 機器學習+消融實驗完整報告
    ├── stage7_dl_report.md         ← 深度學習LSTM完整報告
    ├── stage8_shap_events.md       ← SHAP+事件case study完整報告（figures\fig6~10.png）
    └── stage5/6/7/8_*.csv          ← 各模型逐站成績、逐日預測、SHAP與事件實驗數字
```

**資料來源引用**：臺北捷運各站分時進出量統計OD（臺北市資料大平臺）、政府行政機關辦公日曆表（data.gov.tw）、臺北市歷次天然災害停止上班上課訊息（臺北市資料大平臺）。
